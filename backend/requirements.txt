# VisionTrack AI - backend runtime dependencies
#
# Configured for CPU & Cloud Container compatibility (Render, Docker, Local).
# Uses PyTorch CPU wheel index to maintain low memory usage and fast builds.

--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.6.0
numpy>=1.24.0
opencv-python-headless>=4.9.0.80
torch>=2.2.0
torchvision>=0.17.0
ultralytics>=8.3.0
lapx>=0.5.5
imageio-ffmpeg>=0.4.9
pytest>=8.0.0
