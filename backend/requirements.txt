# VisionTrack AI - backend runtime dependencies
#
# PyTorch is intentionally NOT pinned here: the correct wheel depends on whether
# you want CPU or CUDA inference. Install it first, from the right index:
#
#   CUDA 12.6:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#   CPU only:   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# Then:  pip install -r requirements.txt
#
# If torch/ultralytics are absent the API still boots and reports
# aiAvailable=false, which puts the UI into an explicitly labelled Demo Mode.

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
numpy>=1.24
opencv-python-headless>=4.9
ultralytics>=8.3
lap>=0.5.12          # optimal assignment for ByteTrack / BoT-SORT
imageio-ffmpeg>=0.4.9  # vendored FFmpeg fallback if no system binary is found
